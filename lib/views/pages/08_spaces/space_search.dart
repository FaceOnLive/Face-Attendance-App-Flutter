import 'package:flutter/material.dart';

class SpaceSearchScreen extends StatelessWidget {
  const SpaceSearchScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Search Attendance'),
      ),
    );
  }
}
