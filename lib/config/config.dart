import 'package:flutter/material.dart';

class AppConfig {
  /// Used in the main home page appbar
  static const String appName = 'Attendance';

  /// Used in the main home page appbar
  static const String appSubtitle = 'contact@faceonlive.com';

  /// Primary Colors of the app, if you want to change other colors you can
  /// change in this the core/constants/app_colors.dart
  /// you need to hot restart the app if you change the color
  /// it woudn't reflect the color on hot reload.

  /* <---- Website Color -----> */
  // static const Color primaryColor = Color(0xFFF4623A);

  /* <---- Old color -----> */
  /// Primary Color used for the whole app and the theme
  static const Color primaryColor = Color(0xFF845EC2);
}
